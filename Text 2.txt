/* Grundstil */
body{
  font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  background:#e3f2fd; /* hellblau */
  color:#222;
  margin:0; padding:20px;
  text-align:center;
}
h1{
  background:#ffecb3; /* hellgelb */
  padding:12px 14px; border-radius:10px; margin:0 0 16px;
}
.topbar{display:flex; gap:12px; align-items:center; justify-content:center; margin-bottom:12px; flex-wrap:wrap}
#rollBtn{
  padding:10px 18px; font-size:16px; border:0; border-radius:10px; cursor:pointer;
  background:#4caf50; color:#fff;
}
#rollBtn:disabled{opacity:.6; cursor:not-allowed}
#statusText{font-size:14px; color:#374151}

/* Brett */
#board{
  display:grid;
  grid-template-columns: repeat(6, 60px);
  grid-template-rows: repeat(6, 60px);
  gap:6px; justify-content:center; margin:10px auto 16px; width:max-content;
}
.cell{
  width:60px; height:60px; border:1px solid #b0bec5; background:#fff; border-radius:8px;
  display:flex; align-items:center; justify-content:center; font-size:14px; position:relative;
}
.cell.finish{background:#dcedc8; font-weight:700}
.cell.active{outline:3px solid #ff9800}

/* Karte für Fragen */
.card{background:#fff; border:1px solid #e5e7eb; border-radius:12px; padding:16px; max-width:720px; margin:0 auto}
.hidden{display:none}
.question{font-size:18px; margin-bottom:10px}
.options{display:grid; grid-template-columns:repeat(auto-fit,minmax(160px,1fr)); gap:8px; margin-bottom:8px}
.option{padding:10px; border:1px solid #cbd5e1; border-radius:10px; background:#fff; cursor:pointer}
.option.correct{background:#e6ffe8; border-color:#16a34a}
.option.wrong{background:#ffe5e5; border-color:#dc2626}
.actions{display:flex; gap:8px; justify-content:center; flex-wrap:wrap}
.typing{padding:10px; border:1px solid #cbd5e1; border-radius:8px; min-width:240px}
.primary{background:#4f46e5; color:#fff; border:0; padding:10px 14px; border-radius:10px; cursor:pointer}
.secondary{background:#fff; color:#111; border:1px solid #cbd5e1; padding:10px 14px; border-radius:10px; cursor:pointer}
.feedback{min-height:22px; margin-top:6px; color:#374151}